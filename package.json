{
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "parcel src/index.html --no-autoinstall",
    "build": "yarn build:parcel && yarn build:static",
    "build:parcel": "parcel build src/index.html --no-source-maps",
    "build:static": "cpx \"src/static/**/*\" dist"
  },
  "devDependencies": {
    "@types/react": "^16.9.43",
    "@types/react-dom": "^16.9.8",
    "cpx": "^1.5.0",
    "parcel": "^2.0.0-beta.1",
    "postcss-modules": "^3.2.0",
    "sass": "^1.26.10"
  },
  "dependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "version": "0.0.10"
}
